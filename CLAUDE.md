# Handly — Project Guide (CLAUDE.md)

Handly is a **managed marketplace for home services in Uzbekistan** (plumber, electrician, cleaner, AC technician, handyman…). Customers describe a problem (with photos/video for free AI diagnosis), the platform auto-matches **one** verified master, and payment/warranty/incentives flow through the app. Monetization is **master subscriptions only** (no commission except a 1% tax withholding). UI is Uzbek-first (Russian second).

**The architecture is the source of truth: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).** Read §0.1 for approved MVP decisions and the tax design. **Brand identity: [docs/design/BRAND.md](docs/design/BRAND.md)** (logo — do not redesign) **and [docs/design/DESIGN_SYSTEM.md](docs/design/DESIGN_SYSTEM.md)** (product usage: placement, colors, type, components) — ink/orange checkmark-H identity, final as of 2026-07-21; the old `docs/BRAND.md`/`docs/DESIGN_SYSTEM.md` (blue dot mark) are superseded stubs kept only so old links don't break. Build milestones in order; do not start a future milestone until the current one is approved.

## Current status

- **Milestone 1 (Foundations + Auth & Profiles): DONE & verified (2026-07-19).**
- **Milestone 2 (Requests, AI diagnosis, pricing): DONE & verified (2026-07-20).**
- **Design system rebrand (ink/orange checkmark-H, per docs/design/): DONE (2026-07-21).** Cross-cutting UI pass, not a numbered milestone — see "What the design-system implementation shipped" below.
- **Milestone 3 (Matching & dispatch, PostGIS, real-time offers): DONE & verified (2026-07-21).**
- **Pre-M4 cross-cutting architecture review (security, scalability, API/test/docs, frontend quality): DONE (2026-07-21).** Whole-codebase pass, not a numbered milestone — see "What the pre-M4 architecture review shipped" below.
- **Milestone 4 (Job execution — EN_ROUTE/IN_PROGRESS/COMPLETED/CLOSED, completion evidence): DONE & verified (2026-07-21).**
- **Milestone 5 (Payments & financial system — mock provider, settlement, earnings): DONE & verified (2026-07-21).**
- **Batch 1 (M4 Job execution + M5 Payments) complete.**
- **Batch 2 (Trust, Safety & Marketplace Growth) complete (2026-07-21).**
- **Batch 3 (Operations & Scale) complete (2026-07-22).** Waiting on founder approval before Batch 4. **The batch roadmap below is the corrected, definitive numbering — do not reinterpret milestone/batch numbers from `docs/ARCHITECTURE.md`'s original (pre-Batch-1) roadmap table, which described different scope under those same numbers.**
  - **Batch 4 — Launch Readiness:** infrastructure, monitoring, CI/CD, backups, security hardening, production deployment.

## Monorepo layout (pnpm workspaces + Turborepo)

```
apps/
  api/        NestJS (Fastify) modular monolith — the backend
  web/        Next.js 15 (App Router) PWA — customer/master/admin frontend
packages/
  contracts/  Zod schemas + inferred types shared by web & api (build to dist)
  ui/         Design tokens (from the wireframe palette) + Tailwind preset
  config/     Shared tsconfig base
infra/docker/ docker-compose.yml (Postgres+PostGIS, Redis)
docs/         ARCHITECTURE.md
```

**Module rule:** each API module owns its tables; cross-module access goes through the owning service or domain events — never raw table access. This keeps future service extraction cheap.

## Tech stack

TypeScript everywhere · NestJS 11 (Fastify, no Passport — custom JWT guard) · Prisma 6 + PostgreSQL · Redis (ioredis) for OTP rate-limiting · argon2 (passwords + OTP hashing) · Next.js 15 + React 19 + Tailwind 3 · TanStack Query + Zustand · Zod (shared validation).

## Running locally

**Prereqs:** Node ≥ 20, pnpm. Database via **Docker Compose** (the approved dev setup).

```bash
pnpm install                 # installs deps; postinstall generates the Prisma client
cp .env.example .env         # single ROOT env file (dev defaults already filled in)
pnpm docker:up               # Postgres + Redis via Docker Compose (needs Docker running)
pnpm db:migrate              # applies migrations (Prisma reads the root .env via dotenv-cli)
pnpm db:seed                 # 6 service categories + dev admin
pnpm dev                     # turbo builds contracts, then runs api :3001 + web :3000
```

There is a single `.env` at the repo **root** — the API loads it at runtime, and the
`db:*` scripts load it for Prisma (via `dotenv -e ../../.env`). Do not create a separate
`apps/api/.env`.

- API base URL: `http://localhost:3001/api/v1` · Web: `http://localhost:3000`
- Health check: `GET /api/v1/health` → `{ status, db, redis }`
- **Dev admin (seed):** `+998900000000` / `admin12345`
- **SMS in dev:** `SMS_PROVIDER=mock` prints the OTP to the API log (search `📱 OTP for`). Set `SMS_PROVIDER=eskiz` + Eskiz creds for real SMS.

> Docker Desktop was not installable non-interactively (its installer needs `sudo`). Milestone 1 was verified against a temporary local Postgres/Redis on the same ports. To use the approved Docker path: install Docker Desktop (needs your password), then `pnpm docker:up && pnpm db:migrate && pnpm db:seed`. Both use `localhost:5432` / `localhost:6379`, so don't run Docker and a local Postgres/Redis at the same time.

## Common commands

```bash
pnpm build            # turbo build all (contracts → ui → api → web)
pnpm typecheck        # tsc across the workspace
pnpm --filter @handly/api test        # node:test unit tests
pnpm --filter @handly/api start:dev   # API only, watch mode
pnpm db:studio        # Prisma Studio
```

## What Milestone 1 shipped

**API (`apps/api`)**
- Auth: `POST /auth/register|login|otp/verify|otp/request|refresh|logout|password/request-reset|password/reset`. Phone + password with mandatory SMS OTP; **rotating single-use refresh tokens** in an httpOnly cookie (`handly_rt`), access JWT (15m) in memory.
- Global guards: `JwtAuthGuard` (Bearer, opt out with `@Public()`) then `RolesGuard` (`@Roles(...)`).
- Users/profiles: `GET /me`, `PATCH /me/customer`, `me/master` (skills, experience, bio, service areas, certifications/portfolio metadata, **PINFL encrypted**, `isSelfEmployed`), `me/addresses`.
- Catalog: `GET /categories` (public).
- `TaxProvider` interface + `MockTaxProvider` (+ `SoliqTaxProvider` stub) — computes/records the 1% withholding. **Not wired into any payment flow yet** (that's M4).
- Infra: typed env validation (Zod), Prisma, Redis, AES-256-GCM field crypto, RFC 9457 (`problem+json`) error filter, `ZodValidationPipe`.

**Web (`apps/web`)**
- Screens: `/login`, `/register` (Mijoz/Usta role), `/verify` (6-box OTP, resend countdown), `/forgot`, `/home` (guarded).
- API client (`lib/api.ts`), Zustand session store (access token in memory, silent-refresh bootstrap), TanStack Query, UI kit in `components/ui`.
- Design tokens in `packages/ui` (brand accent, ink `#111418`, warm neutrals) — light + dark, theme-aware. **Brand accent is now orange `#F26A1B`** per the 2026-07-21 rebrand (was blue `#185FA5` at M1 ship time) — see below.
- Support contacts: single source `apps/web/lib/support.ts` (`SUPPORT_PHONES`) rendered via `<SupportContacts />` as tappable `tel:` links — auth footer + `/help` today; Admin settings (M5) must read the same constant.

## What Milestone 2 shipped

**API (`apps/api/src/modules/orders`, `ai`)**
- Orders: `POST /orders` (draft), `PATCH /orders/:id`, `POST /orders/:id/media` (multipart, `@fastify/multipart`), `DELETE /orders/:id/media/:mediaId`, `POST /orders/:id/diagnose`, `POST /orders/:id/submit` (requires `consent:true`), `POST /orders/:id/cancel`, `GET /orders` (cursor-paginated), `GET /orders/:id`. Public `GET /media/:id` streams uploaded files (unguessable-uuid trust model, same as a presigned URL).
- State machine (`order-state.ts`): `DRAFT → PRICED → SEARCHING`, editing a `PRICED` order's category/description/tier drops it back to `DRAFT` (must re-diagnose); cancel allowed from `DRAFT`/`PRICED`/`SEARCHING`. Enforced server-side only — the frontend's cancel-button visibility is a UI affordance, not the authority.
- Pricing (`pricing.ts`): deterministic — category base band × complexity multiplier × service-tier multiplier, rounded to 5 000 so'm, floored at 30 000. **The AI never sets price directly**, only complexity.
- AI diagnosis (`ai/`): `AiService` picks `ClaudeAiProvider` (vision + `messages.parse` structured output, model from `AI_MODEL` env) when `AI_PROVIDER=claude` + a key is set, and **always falls back to `MockAiProvider`** (deterministic uz/ru keyword heuristic) on any failure or when unconfigured — AI assists, never gates, per architecture.
- Storage (`infra/storage`): `StorageProvider` interface + `LocalDiskStorage` (dev). S3 provider slots in later without touching callers.
- Service tiers: `SCHEDULED` (free, fixed slots `09:00/11:00/14:00/16:00`, ≤14 days ahead), `PRIORITY` (+15 000 so'm), `EMERGENCY` (+30 000 so'm) — metadata in `packages/contracts/src/order.ts` (`SERVICE_TIER_INFO`).
- **Timezone correctness:** Uzbekistan is a fixed UTC+5 offset (no DST). Slot math (`slotToUtcIso`/`utcIsoToSlot` in contracts) uses `Date.UTC()`/`getUTC*()` exclusively — never local `Date` methods — so scheduling is correct regardless of the server's or browser's own system timezone. Covered by dedicated round-trip unit tests.

**Web (`apps/web/app/orders`, `components/order`)**
- `/orders/new` — 3-step wizard (category + description + media + GPS/address → service tier + date/slot → AI quote + consent + submit), state persisted to `sessionStorage` (`lib/wizard-store.ts`) so a refresh resumes at the right step; the order itself stays server-authoritative.
- `/orders` — list with status badges + price range. `/orders/[id]` — full detail: media, AI diagnosis, price, status timeline, conditional cancel/resume actions.
- `/home` redesigned: real category grid (`GET /categories`), tap-through to the wizard pre-selecting that category.
- `components/nav/bottom-nav.tsx` — 5-tab nav (Bosh/Buyurtmalar/+/Bildirishnoma/Profil); `/profile` (moved from the old `/home`) and `/notifications` (placeholder) fill out the remaining tabs.

## What the design-system implementation shipped (2026-07-21)

Cross-cutting UI pass implementing the approved design handoff — not a numbered milestone; no backend or functional changes, M1/M2 behavior preserved throughout.

- **Source of truth relocated:** `docs/design/` (BRAND.md, DESIGN_SYSTEM.md, `components/COMPONENTS.md`, `tokens/handly-tokens.css`, `logos/`, `screens/*.dc.html` reference screens, `exports/`) is now canonical, copied in from the design handoff per its own manifest. The old root `docs/BRAND.md`/`docs/DESIGN_SYSTEM.md` are now 3-line stub pointers (kept only so existing links don't 404).
- **Rebrand:** logo mark changed from an ink tile + dot-in-H to a bare two-tone mark — ink "H" whose crossbar rises into an **orange checkmark** (`#F26A1B`) — per `docs/design/BRAND.md`, which explicitly marks this final ("do not revert to blue"). Updated `packages/ui/src/tokens.css` (brand ramp + all `--color-primary*`/`--color-focus`/`--color-info*` slots), `components/ui/logo.tsx`, `app/icon.svg`. **Dark-theme brand values aren't specified upstream** (the handoff's token sheet is light-only) — retinted here by re-applying the same light→dark derivation the previous blue ramp used (one step lighter for `-hover`, dark desaturated for `-soft`, etc.), so dark mode keeps working; flag to design if they want to specify these explicitly later.
- **Nav kept at 5 tabs:** the reference screens show a 4-tab nav (Home/Bookings/Chats/Profile, no center button) — but that drops the only visible way to reach `/orders/new` and the Notifications tab, with nothing shown to replace either. Kept the current 5-tab structure (incl. the center "+" create button) and just re-skinned colors/icons, rather than regressing that navigation path. No "Chats" tab/feature was added (no chat backend exists).
- **New components:** `Chip` and `Rating` in `components/ui/badge.tsx`; `components/master/` (`OnlineToggle`, `RouteTimeline`, `BookingRequestCard`, `WeekEarningsChart`) for the new Master Dashboard screen.
- **New screen — Landing (`/`):** logged-out root is now a real marketing page (`components/landing/marketing-landing.tsx`) per `Handly Landing.dc.html` — hero, real `GET /categories` service grid (real names + `basePriceMin`, not fabricated), how-it-works, trust panel, masters CTA, footer. Logged-in users still redirect straight to `/home` as before.
- **New screen — Master Dashboard (`/master`):** static/presentational recreation of `Handly Master Dashboard.dc.html` (online toggle, incoming-request accept/decline with countdown, route timeline, weekly earnings chart). **All data and interactions are local demo state only — no backend wiring** (matching/dispatch is M3, earnings ledger is M4+, neither exists yet). Reachable via a "Usta paneli" link on `/profile`, shown only for `role === 'MASTER'`. Treat as a visual preview, not a functional dashboard, until the relevant backend milestones land.
- **`/home` re-skinned, not re-built:** kept the real, working sections (search bar, category grid, greeting) restyled to the new palette/icons; did **not** add the reference's fabricated "active booking tracker" (hardcoded master/ETA), "top masters nearby" list, or promo banner, since Handly has no live-matching, master-ranking, or promo system yet and faking that data in a production screen would be misleading.

## What Milestone 3 shipped

**API — new modules (`apps/api/src/modules/dispatch`, `notifications`, `realtime`; `infra/push`, `infra/queue`)**
- **Dispatch engine (`modules/dispatch`):** `DispatchService.startDispatch(orderId)` runs right after `OrdersService.submit()` sets `SEARCHING` (fire-and-forget from the caller's perspective — a slow/failed cascade never fails the customer's submit request). Eligibility (`dispatch-eligibility.ts`) is one indexed raw-SQL query — not N+1 — filtering by category skill, the master's own `ServiceArea.radiusM` via `ST_DWithin` (PostGIS), `verificationStatus = VERIFIED`, `isOnline`, `trustTier` (`requiredTierForComplexity`: SIMPLE→0/MEDIUM→1/COMPLEX→2/CRITICAL→3), **not already working another `ASSIGNED` order**, no `BUSY`/`BOOKED` `AvailabilitySlot` for today, and not already offered this specific order. Scoring (`dispatch-scoring.ts`): `0.5×distance + 0.35×rating + 0.15×jobsDone`, weighted-random pick among the top-N closest (`DISPATCH_TOP_N`, default 5) — not a strict "closest always wins," per the approved sequential-dispatch design. One offer at a time; on decline or expiry, `cascadeNext` re-runs eligibility (excluding already-offered masters) and offers the next candidate; if the pool is exhausted, the radius widens once (`DISPATCH_RADIUS_EXPANSION_FACTOR`, default 2×) before the order terminally becomes `EXPIRED`.
- **Accept is the one truly race-sensitive write:** `acceptOffer` runs inside `prisma.$transaction` with `SELECT ... FOR UPDATE` on the order row first, so a concurrent accept on a *different* dispatch for the same order blocks until the winner commits, then correctly sees the order is no longer `SEARCHING` and is rejected with `409`. All other `OFFERED` dispatches for that order flip to `WITHDRAWN` in the same transaction. `OrderDispatch` has `@@unique([orderId, masterId])` (a master can never be double-offered the same order) and BullMQ jobs use deterministic `jobId`s (`offer-expiry-<dispatchId>`) so retried/duplicate enqueues can't create duplicate timers.
- **Background jobs (`infra/queue`):** one BullMQ queue (`dispatch`) on its own Redis connection (`maxRetriesPerRequest: null`, BullMQ's hard requirement — the shared `RedisService` connection caps retries for OTP rate-limiting, which is wrong for a queue). `DispatchWorker` processes `offer-expiry` jobs, calling the same idempotent `handleOfferExpiry` the decline path can also trigger indirectly (an `updateMany` status-guard means a second call on an already-resolved dispatch is a safe no-op).
- **Real-time (`modules/realtime`):** Socket.IO gateway at `/ws`, JWT-authenticated on handshake (reuses the same verification `JwtAuthGuard` does — no duplicated logic), joins a `user:${userId}` room. `DispatchService` calls `RealtimeGateway.emitToUser(...)` directly (no event bus exists anywhere in the API; introducing one for a single use case would be new architecture, not reuse). Events: `offer:received` (master), `order:updated` (customer + master, on assign/expire).
- **Notifications (`modules/notifications`):** `createRecord(tx, ...)` (transactional DB write, always inside the same transaction as the state change it announces) vs. `deliver(notification)` (best-effort push + socket, fire-and-forget after commit) vs. `notify(...)` (convenience wrapper for non-transactional call sites). `GET /notifications` (cursor-paginated, same pattern as `GET /orders`), `GET /notifications/unread-count`, `PATCH /notifications/:id/read`.
- **Push abstraction (`infra/push`):** `PushProvider` interface, `MockPushProvider` (console-logs, dev default), `FcmPushProvider` (`firebase-admin`, real send when `PUSH_PROVIDER=fcm` + all three `FCM_*` vars are set) — same "assists, never blocks" fallback shape as `AiService`. **Not yet wired to real device tokens** (no push-token registration endpoint exists yet — that's whenever the mobile/PWA push-permission flow is built); today it only ever exercises the mock path in practice.
- **New endpoints on the existing `master.controller.ts`:** `PATCH /me/master/availability {isOnline}`, `GET /me/master/offers/current`, `POST /me/master/offers/:id/accept`, `POST /me/master/offers/:id/decline`, `GET /me/master/current-job`.
- **`order-state.ts`:** `SEARCHING` now also transitions to `ASSIGNED` (dispatch success) and `EXPIRED` (pool exhausted); new `ASSIGNED → CANCELLED_BY_CUSTOMER` (customer can still back out post-match — no execution/payment has happened yet, that's M4).
- **PostGIS is live:** `orders.location` and `service_areas.centerPoint` are Postgres **generated columns** (`GENERATED ALWAYS AS (...) STORED`, `geography(Point,4326)`), auto-derived from the existing decimal lat/lng — modeled in `schema.prisma` as `Unsupported("geography(Point, 4326)")?` so Prisma knows the columns exist but never writes them. GIST indexes on both. The migration adds `CREATE EXTENSION IF NOT EXISTS postgis;` itself (needed for Prisma's shadow DB, which doesn't have it enabled by default even though the real dev DB does).

**Web**
- **`/master` is now real** (was a static demo, explicitly labeled as such): `useQuery`/`useMutation` against the endpoints above, `useSocketEvent('offer:received' | 'order:updated', ...)` (new `lib/socket.ts`, a thin `socket.io-client` singleton) updating query cache directly, real countdown from `offer.expiresAt`. Removed the fabricated "340k today / 3–5 jobs / 98% acceptance" stat strip and the `WeekEarningsChart` usage (no earnings ledger exists before M4) — replaced with the master's real `ratingAvg`/`jobsDone`. `RouteTimeline` now shows the one real current `ASSIGNED` job, or an honest empty state.
- **`/orders/[id]`:** subscribes to `order:updated` and refetches live; new "assigned master" card (name, verified check, rating) when `status === 'ASSIGNED'`; `EXPIRED` gets its own status message.
- **`/notifications`:** real list + mark-as-read-on-tap (was a placeholder).
- **Bottom nav:** real unread-count badge on the Bildirishnoma tab, live-updated via socket (replaces the old fake badge that only existed in the static master-dashboard nav).

**Real bugs found and fixed during M3's own verification pass** (not tooling quirks — see "Known issues" below for those):
- **BullMQ custom job IDs can't contain `:`.** The first `offerExpiryJobId`/`cascadeNextJobId` implementation built IDs like `` `offer-expiry:${dispatchId}` `` — BullMQ rejects any custom `jobId` containing a colon (it's BullMQ's own internal key delimiter) with `Error: Custom Id cannot contain :`. Because the cascade call is fire-and-forget from `OrdersService.submit()`, this exception was swallowed by the `.catch()` logger and never surfaced as a request failure — every single dispatch offer silently got **no expiry timer at all** (offers would sit `OFFERED` forever; `handleOfferExpiry`/cascade-on-timeout never ran). Confirmed via an empty `bull:dispatch:delayed` Redis set after a real dispatch. Fixed by switching both ID helpers to `-` (`apps/api/src/infra/queue/queue.constants.ts`); re-verified by watching a real offer both get its delayed job enqueued in Redis *and* fire exactly on schedule (dispatch flipped `OFFERED → EXPIRED` 34ms after `expiresAt`). If any future job type adds a custom `jobId`, keep it colon-free.
- **Eligibility didn't exclude masters already working an active job.** `findEligibleCandidates` checked `isOnline`/verification/trust-tier/today's `AvailabilitySlot`, but nothing excluded a master who already had a different order `ASSIGNED` to them — so a master mid-job could still be offered (and accept) a second, unrelated job, breaking the "one master, one job at a time" premise the whole sequential-dispatch design assumes. Confirmed live (a busy master received a fresh offer for an unrelated order) before being fixed. Fixed with a `NOT EXISTS (SELECT 1 FROM orders WHERE "masterId" = mp."userId" AND status = 'ASSIGNED')` predicate in `dispatch-eligibility.ts`, plus a dedicated regression test (`dispatch-integration.test.ts`: "excludes a master currently working another ASSIGNED job").

**Post-ship production-readiness audit (2026-07-21, before M3 approval)** — five areas verified with live evidence, not just code review; two more real issues found and fixed:
- **Concurrency (verified, no fix needed):** two real HTTP `accept` requests fired truly concurrently (backgrounded shell jobs) against two competing `OFFERED` dispatches for the same order — exactly one returned `201`, the other `409`, and the loser's dispatch row was cleanly `WITHDRAWN` (not left dangling). Confirms the `SELECT ... FOR UPDATE` transaction in `DispatchService.acceptOffer` serializes correctly under real concurrent load, not just in the existing DB-transaction unit test.
- **Queue resilience — real gap found and fixed.** Redis's dev image only did periodic RDB snapshots (`save 3600 1 300 100 60 10000`, no AOF) — confirmed a `SIGKILL`'d Redis container **lost** a pending offer-expiry BullMQ job created after the last snapshot, leaving its `OrderDispatch` stuck `OFFERED` past `expiresAt` forever (no self-healing exists for this). A *graceful* restart survived fine (Redis saves on `SIGTERM` when save points are configured) and the app auto-reconnects either way (ioredis's default retry strategy) — it's specifically the ungraceful-crash case that lost data. Fixed by enabling AOF (`--appendonly yes --appendfsync everysec`) in `infra/docker/docker-compose.yml`; re-ran the identical `SIGKILL` test and the job now survives and fires exactly on schedule. **Residual, accepted limitation:** there's still no periodic reconciliation sweep for the (now much rarer) case of a lost job — a good candidate for M4+ hardening, not built here since it's a new job type/design decision, not a config fix.
- **Socket.IO reconnection (verified, no fix needed):** forced a real disconnect (killed the API process) while a customer's order-detail page was open; the client auto-reconnected with a fresh session **twice in a row** with zero page action (Socket.IO's default `reconnection: true`, and `lib/socket.ts`'s `auth` callback re-evaluates the access token on every attempt). The server's `handleConnection` re-verifies the JWT and rejoins `user:{id}` on every new connection by construction (no reconnect-specific code needed). A subsequent real `accept` still pushed `order:updated` to the page live, no refresh.
- **Database performance — real gap found and fixed.** `EXPLAIN ANALYZE` against a synthetic 5,000-master dataset (scattered across Uzbekistan's real geographic extent, not just clustered near Tashkent — a tight scatter box hides this) showed the GIST index on `service_areas.centerPoint` was **never used** — Postgres seq-scanned every service area on every dispatch call. Root cause: `ST_DWithin(sa."centerPoint", o."location", sa."radiusM" * multiplier)` has a **per-row** radius (`sa.radiusM` is a column), and PostGIS can only drive a GIST index scan off a *constant* radius. Fixed by adding a constant-radius `ST_DWithin(..., 50_000 * multiplier)` pre-filter alongside the existing exact check (`dispatch-eligibility.ts`) — `50_000` mirrors the Zod-validated max `ServiceArea.radiusM` (`packages/contracts/src/user.ts`). Re-verified: the GIST index is now used (`Index Scan using service_areas_center_point_gist`), ~4× fewer buffer reads at 5,000 rows, and the real (non-benchmark) code path still returns the correct nearest real candidate. This gap would only have worsened as the real master count grew — buffer cost scaled with *total* masters before the fix, with *nearby* masters after.
- **API/contracts consistency (verified, no fix needed):** every M3 endpoint, request/response DTO, and Socket.IO event name cross-checked field-by-field between `packages/contracts`, the NestJS controllers, and every `apps/web/lib/*.ts` caller — all matched exactly, no orphaned frontend calls to non-existent routes. Minor, non-blocking note: `OrderUpdatedEvent`, `DispatchStatus`, `dispatchStatusSchema`, and `notificationTypeSchema` are exported but not currently imported anywhere (the two socket call sites use inline types instead; the status/type schemas have no user-input validation call site yet) — left as-is since they're harmless and consistent with this milestone's "leave extension points for later" pattern, not deleted per "don't modify unless blocking."

**Deliberately scoped down vs. the full ARCHITECTURE.md §9 vision** (all additive/extensible, not corners cut): no hot-reloadable rules engine (plain typed constants in `env.ts` instead — swapping to a real rule-config lookup later is a one-file change); no dynamic trust-tier recompute worker (`trustTier` stays admin/seed-set until M4+'s completed-job data exists to drive it); no Premium-first cascade round (no subscriptions exist yet — scoring has no Premium bit to check); no `EN_ROUTE`/`IN_PROGRESS`/`COMPLETED`/live GPS tracking (`ASSIGNED` is the new terminal status; job execution and payment are M4). `OrderDispatch.countedAsLead` **is** included and correctly set to `true` on accept, per "leave clean extension points" — nothing reads it yet, but M5's lead-quota system will read a field that was already being populated correctly.

## What the pre-M4 architecture review shipped (2026-07-21)

Whole-codebase review (not a numbered milestone) covering security, scalability, database, API/test/docs consistency, and frontend quality across M1–M3 before starting M4. Three parallel research passes (auth/crypto/validation/injection; schema/indexes/N+1/pooling; controller consistency/test gaps/docs accuracy/accessibility) surfaced real findings; the ones that were genuinely production-critical and low-risk to fix were fixed and verified live, the rest documented below as deliberate follow-ups.

**Fixed, with real evidence:**
- **Placeholder secrets could silently reach production.** `JWT_ACCESS_SECRET`/`FIELD_ENCRYPTION_KEY` were only length-checked, so the shipped `.env.example` dev values (`"dev-access-secret-change-me"` etc.) could run in prod undetected. `loadEnv()` (`infra/config/env.ts`) now refuses to start when `NODE_ENV=production` and either secret matches a known placeholder or is under 32 chars — verified all three cases (placeholder+prod → throws, real secret+prod → boots, placeholder+dev → boots unaffected).
- **No brute-force protection on `/auth/login`.** Reused the exact Redis counter pattern OTP already uses (`incrWithTtl`/`LOGIN_MAX_ATTEMPTS` default 10, `LOGIN_LOCKOUT_SECONDS` default 900) in `auth.service.ts`. Verified live: 10 wrong-password attempts lock the phone out (11th+ gets a clear "too many attempts" message, checked *before* the argon2 hash cost is even paid), a correct password is also blocked while locked, and a subsequent successful login clears the counter.
- **Refresh-token rotation had a real race.** `findValidSession` + `revokeById` were two separate, unlocked calls — two concurrent `/auth/refresh` requests with the same raw token could both validate before either revoked, minting two live sessions from one single-use token. Replaced with `TokenService.consumeSession`, a single `SELECT ... FOR UPDATE` transaction mirroring `DispatchService.acceptOffer`'s already-proven pattern. Verified two ways: a new `token-refresh-race.test.ts` (DB-backed, mirrors `dispatch-integration.test.ts`'s accept-race test) and two genuinely concurrent real HTTP `/auth/refresh` calls with the same cookie — exactly one `200`, one clean `401`, exactly one new session minted.
- **Two missing indexes on hot paths.** `Session.refreshTokenHash` had no index at all (full scan on every refresh/logout); `Order` had separate single-column `masterId`/`status` indexes instead of a composite, hitting the dispatch "busy master" check on every cascade step (the same class of gap as the M3 audit's PostGIS fix, just a plain btree this time). Added both via migration `20260721132546_m3_audit_indexes`; confirmed present via `\d`. Small-table caveat applies same as before — Postgres correctly seq-scans at current row counts; the indexes are there for when that stops being true.
- **No baseline security response headers.** Added `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin` via one Fastify `onSend` hook in `main.ts` — no new dependency. Verified present on both a JSON API response and the media-serving endpoint. CSP/HSTS deliberately left out (CSP needs real tuning against every asset source first; HSTS is normally the reverse proxy/CDN's job once TLS terminates there).
- **No React error boundary anywhere.** An unhandled render error fell through to Next.js's raw default error screen. Added `apps/web/app/error.tsx` (styled, reuses `Logo`/`Button`, "retry" + "go home") and `apps/web/app/global-error.tsx` (bare-bones fallback for the rarer case where the root layout itself fails, per Next's own requirement that it ship its own `<html>/<body>`). Verified live via a temporary throwing test route.
- **Fetch failures were invisible across most of the app.** `orders`, `orders/[id]`, `notifications`, `home` (categories), and `master` (profile) all either showed the loading skeleton forever or silently rendered as if the list were empty on a real fetch failure — no page but the order wizard checked `isError`. Added `isError`/`refetch` handling reusing the existing `Alert` component (same one already used for the cancel-order error) to all five. Verified live against a real backend error (not just code review) — caveat found and worth knowing: TanStack Query's default `networkMode:'online'` means a *fully unreachable* server (connection refused, not an HTTP error response) is treated as "probably offline" and the query sits in `fetchStatus:'paused'` rather than ever reaching `isError` — confirmed via direct query-state inspection. That's intentional framework behavior (it auto-resumes once back online) and is a different, narrower case than what this fix targets (real 4xx/5xx responses, which do reach `isError` normally); handling the paused/offline case explicitly is a small, clearly-separable follow-up, not done here.
- **Four accessibility gaps**, each copying a pattern already used correctly elsewhere in the same codebase: the order wizard's address field had no `<label>` (added one, matching every other field); the schedule step's native date input had no accessible name (added `aria-label`); the consent toggle had no `aria-pressed` (added it, matching `OnlineToggle`'s existing pattern); the master dashboard's "mark complete"/call actions were unfocusable `<span>`s styled like buttons — converted to real `disabled` `<button>`s (they're intentionally non-functional placeholders per the M3/M4 boundary, so `disabled` is the honest state, not a fake-interactive span).
- **A handful of stale `docs/ARCHITECTURE.md` claims**, all now corrected: M3 wasn't marked done in the status line or the roadmap table (the table's M3 row still described the original full aspirational scope rather than what actually shipped); the AI model name was hardcoded to a stale value in two places (now points at the `AI_MODEL` env var); accept's race-safety was described as `SKIP LOCKED` in two places when the actual, tested mechanism is a blocking `FOR UPDATE` (the opposite technique — skip-and-grab-another vs. serialize-on-the-same-row); the dispatch hot-path description said "one" `ST_DWithin` when the M3 audit's own fix added a second (constant-radius pre-filter) alongside it.

**Verified with no fix needed:** password hashing (argon2id, timing-safe verify), PINFL field encryption (fresh IV per call, auth tag actually checked on decrypt), every `@Body()` validated via `ZodValidationPipe`, all three raw-SQL call sites parameterized (no injection surface), file uploads (server-side MIME/size re-validation, UUID object keys, no path traversal), CORS locked to `WEB_ORIGIN`, error responses never leak stack traces/internals, `.env` correctly gitignored with no committed secrets, zero N+1 query patterns anywhere in the API, all migrations safe (no `NOT NULL` column added without a default on a populated table), full compliance with the integer-so'm-never-floats money convention.

**Deliberately deferred, not fixed now** (real, but broader in scope than a targeted fix, or lower marginal urgency than the items above — flagged for a future dedicated pass, not silently dropped): backfilling test coverage for `auth.service`'s and `dispatch.service`'s own methods and `mock-ai.provider`'s heuristic (currently only their pure helpers/SQL are directly tested); file-upload magic-byte content sniffing (client-supplied MIME is trusted; not an active exploit path today since nothing serves uploads for inline execution, but worth closing before that changes); relocating `master.controller.ts`'s locally-defined `mediaSchema` into `packages/contracts` (a convention nit, zero functional impact); Prisma connection-pool tuning (unconfigured, running on bare defaults — not yet a bottleneck at current scale); a `TanStack Query` `isPaused`/offline-specific UI state (see the fetch-failure fix above).

## What Milestone 4 shipped (2026-07-21)

**API — `order-state.ts` extended, no new modules** (job execution reuses the existing orders/notifications/realtime modules):
- **State machine:** `ASSIGNED → EN_ROUTE → IN_PROGRESS → COMPLETED → CLOSED`, one master action per step (en-route, start, complete) plus a customer confirmation (CLOSED). Cancellation stays allowed through `EN_ROUTE` (customer can still back out before work starts) but not once `IN_PROGRESS`. `CANCELLED_BY_MASTER`/`DISPUTED` stay unreachable (pre-declared enum values, no flow requested this batch) — same pattern M3 used for `EXPIRED` ahead of its own build.
- **New endpoints:** `POST /orders/:id/confirm` (customer, CLOSED); `POST /me/master/current-job/:id/en-route|start|complete`, `POST /me/master/current-job/:id/media` (multipart completion-evidence upload), `GET /me/master/jobs` (job history).
- **Completion evidence reuses `OrderMedia`** (not a parallel model) — one new `uploadedByRole` (`CUSTOMER`|`MASTER`) discriminator column distinguishes the customer's initial problem photos from the master's completion evidence in the same table/endpoint family.
- **Settlement bridge:** `Order.finalAmount` (nullable Int) — the master-entered actual price at completion, validated server-side to fall within the already-quoted `[priceMin, priceMax]`. This is what M5's `Payment.amount` charges.
- **`jobsDone` increments at customer CONFIRMATION** (the `CLOSED` transition), not at the master's own `COMPLETED` self-report — a deliberate trust-signal design choice (the master's own claim isn't taken as verified until the customer agrees).
- **Dispatch eligibility and `getCurrentJob` both widened** from "busy = `ASSIGNED`" (M3) to "busy = `ASSIGNED`/`EN_ROUTE`/`IN_PROGRESS`/`COMPLETED`" — a master mid-job (or awaiting the customer's confirmation) must stay ineligible for a new dispatch offer and must still see the job as their "current job" until it's actually `CLOSED`. Missing this would have broken the "one master, one job" invariant M3 already established.

**Real bugs found and fixed during M4's own build/verification:**
- **Duplicate-transition race in the new job-execution writes.** The first cut of `startEnRoute`/`startService`/`completeService`/`confirmCompletion` read the order's status, then wrote unconditionally — two concurrent calls (a double-tapped "mark complete" button) could both pass the check and both write, creating duplicate `statusHistory` rows and duplicate notifications. Fixed with a guarded conditional update (`updateMany({where: {id, status: expectedFromStatus}})`, checking `count === 1` before proceeding) — the same race class already fixed for `DispatchService.acceptOffer` (M3) and `TokenService.consumeSession` (pre-M4 audit), just via a guarded update instead of `SELECT...FOR UPDATE` since there's only ever one legitimate writer (the assigned master) racing against their own duplicate request. Verified with a dedicated concurrent-call test for both `startEnRoute` and `completeService` — exactly one of two simultaneous calls succeeds, exactly one `statusHistory` row is created.
- **Frontend `api.post(path, undefined, true)` sent `Content-Type: application/json` with no body**, which Fastify's body parser rejects with `400 Bad Request: "Body cannot be empty..."`. This is a pre-existing bug in `apps/web/lib/api.ts` (not new to M4) that had never been hit by a real browser click before — M3's own verification of `acceptOffer` used curl, not the UI button. Caught live while clicking "Qabul qilish" (accept offer) in the browser for real the first time. Fixed by only setting the `Content-Type` header when a body is actually present. This affects every `api.post` call with no body across the whole app (accept/decline offer, diagnose, cancel, confirm, and all four new M4 job-actions) — all now verified working via real browser clicks end-to-end.

**Deliberately scoped down:** no live GPS en-route tracking (`EN_ROUTE` is a status, not a location stream); no master-initiated cancellation or dispute flow (`CANCELLED_BY_MASTER`/`DISPUTED` stay unreachable); no auto-close-after-timeout worker for a customer who never confirms (`COMPLETED` just sits until they do).

## What Milestone 5 shipped (2026-07-21)

**API — new `infra/payment/` (provider abstraction) + `modules/payments/` (business logic + endpoints):**
- **`PaymentProvider` interface + `MockPaymentProvider`** — same interface+mock+facade shape as every other external-service abstraction in this codebase (`StorageProvider`, `PushProvider`, `TaxProvider`, `AiProvider`). Only `MOCK` is implemented (no real Click/Payme/Uzum merchant credentials exist); `PaymentMethod` still models all four so the customer-facing selection UI and data are provider-aware ahead of real credentials, per the explicit "don't implement real provider credentials" scope boundary. Selected via `PAYMENT_PROVIDER` env var (currently only `mock` is a valid value), matching the `TAX_PROVIDER`/`AI_PROVIDER`/`PUSH_PROVIDER` config-driven-selection convention.
- **`Payment` model** (schema added pre-M4, wired now): `amount`/`platformFee`/`taxAmount`/`masterNetAmount` all integer so'm; `taxRate` is the one legitimate `Decimal` (a rate, not currency). Settlement math: `masterGross = amount - platformFee`; `TaxProvider.computeWithholding(masterGross)` (the same M1-built 1% withholding interface, wired into a real flow for the first time) yields `taxAmount`/`masterNetAmount`.
- **Endpoints:** `POST /orders/:id/payments` (customer initiates — body is only `{method}`, never a client-supplied amount; the charged amount is always server-computed from `order.finalAmount`), `GET /orders/:id/payments` (history for that order), `POST /payments/webhook` (public, for async provider confirmation — see below), `GET /me/master/earnings` (summary + transaction history).
- **Duplicate-payment protection:** `initiate()` runs inside a transaction that locks the order row (`SELECT...FOR UPDATE`) before checking for an existing active (`PENDING`/`PROCESSING`/`SUCCEEDED`) payment — the same pattern as `DispatchService.acceptOffer`. Caught and fixed a real instance of the "read-then-write without a lock" race during this milestone's own build (before it ever ran against real concurrent traffic) — a first draft checked-then-created without locking, which two truly concurrent `initiate()` calls could both pass before either committed. Verified with a dedicated concurrent-call test: exactly one of two simultaneous `initiate()` calls on the same order succeeds; exactly one `Payment` row exists afterward.
- **Webhook is a documented, not-yet-exercised extension point.** `providerRef` acts as an unguessable bearer token — the same trust model already established for `GET /media/:id` (per M2's own design) — since no real merchant account (and its signature scheme) exists yet. `MockPaymentProvider` settles synchronously inside `initiate()` itself, so the webhook path is dead code in practice today, exactly like `FcmPushProvider`'s real-send path from M3 ("not yet wired to real device tokens... today it only ever exercises the mock path"). Before enabling a real async rail, add that provider's signature verification to the webhook handler.
- **Failure is reported truthfully, never silently swallowed.** Unlike the "assists, never gates" fallback shape (`AiProvider`, `PushProvider`), a declined charge is recorded as `FAILED` with its reason, not silently retried as success — money-correctness takes priority over the assists-pattern here. Verified with a dedicated test using a stub provider that always declines.

**Web:**
- **`/orders/[id]`** — new "To'lov" (payment) section, shown once the order is `COMPLETED`/`CLOSED`: method selector (`Sinov to'lovi`/Click/Payme/Uzum Bank chips) + pay button when no active payment exists; a settled/pending payment replaces the selector with its status badge and amount; a `FAILED` payment shows its reason and re-offers the selector (retry). Payment history (older attempts) listed below.
- **New screen — `/master/earnings`** — total net earnings + jobs-paid count, transaction list (method, date, net amount). Linked from `/master` and `/profile` (masters only), same placement pattern as the M4 `/master/jobs` link.
- **Verified live, end-to-end, in a real browser**: customer pays via the mock rail → status flips to `SUCCEEDED` instantly → the payment section correctly stops offering the method selector (duplicate-payment guard reflected in the UI, not just the API) → master's `/master/earnings` shows the correct net amount (spot-checked against the settlement formula by hand).

**Deliberately not implemented, per explicit scope:** real Click/Payme/Uzum merchant credentials/signature verification; subscriptions; cashback; referrals; wallet/withdrawal features (all later milestones per the roadmap — `PaymentProvider`/`TaxProvider` exist so they slot in without rework).

## What Batch 2 shipped (2026-07-21)

Trust, Safety & Marketplace Growth — 7 new backend subsystems (`modules/trust`, `verification`, `reviews`, `guarantee`, `penalties`, `referrals`, `subscriptions`, `analytics`, `admin`), all additive to Batch 1; no existing endpoint, contract field, or DB column changed meaning.

**Trust System (`modules/trust`)** — `TrustService.computeTrustScore` is a deterministic, explainable weighted formula (jobsDone capped at 50, ratingAvg/5, responseRate, 1−cancellationRate, a verification bonus, each with a fixed weight summing to 100) minus a capped penalty deduction, mapped to tier 0–3 via fixed thresholds in `trust-config.ts` — **no AI, no cron**. Recomputed only at explicit trigger points (job close/confirm, review posted, penalty issued, verification decided) via `recomputeTrustTier`, plus on-demand on `GET /me/master/trust` — the same "recompute on read, no background worker" shape used elsewhere in this codebase.

**Master Verification Workflow (`modules/verification`)** — `POST/GET /me/master/verification` (submit → `PENDING`, resubmit blocked while pending or already `VERIFIED`) and `POST /admin/verifications/:id/decide` (`@Roles('ADMIN')`, approve/reject with a required note on rejection) — syncs `MasterProfile.verificationStatus` and triggers a trust recompute. No admin dashboard UI was built (explicitly out of scope this batch) — the decide endpoint is dashboard-ready, just not fronted by one yet.

**Handly Guarantee (`modules/guarantee`)** — one `GuaranteeClaim` per order (`@unique` on `orderId`, same pattern as `Payment`/`OrderDispatch`), eligible only for a `CLOSED` order with at least one `SUCCEEDED` payment (the "digital payment requirement") from a verified master. `POST/GET /orders/:id/guarantee-claim` (customer) + `POST /admin/guarantee-claims/:id/decide` (admin). No dispute-resolution flow — `UNDER_REVIEW` exists as a pre-declared, currently-unreached status for future use, same pattern as `DISPUTED` on `Order`.

**Penalty Engine (`modules/penalties`)** — a single `PENALTY_RULES` config table (`penalty-rules.ts`) is the only source of point values/severities for all 5 event types (`CANCELLATION`, `NO_SHOW`, `LATE_RESPONSE`, `POOR_QUALITY`, `CUSTOMER_COMPLAINT`) — every call site reads from it, nothing hardcodes a point value inline. Triggers: master cancellation (new `cancelByMaster`, reachable from `ASSIGNED`/`EN_ROUTE` only — `IN_PROGRESS` can no longer be backed out of), dispatch offer **timeout only** (`handleOfferExpiry`) — a deliberate choice **not** to penalize an honest `declineOffer`, so masters aren't discouraged from declining honestly in favor of silent non-response — and a ≤2-star review (`CUSTOMER_COMPLAINT`). Never auto-bans/suspends — `MasterProfile.status` is untouched by any penalty; only the trust score/tier is affected.

**Cashback & Referral Foundation (`modules/referrals`)** — 3-state `ReferralStatus` lifecycle (`PENDING` on signup with a valid `referredByCode` → `ACTIVE` on the referee's OTP verification → `REWARDED` on the referee's first `SUCCEEDED` payment, guarded by the status-transition race pattern so a concurrent double-settle only rewards once). Fixed `REFERRAL_REWARD_AMOUNT = 20 000` so'm cashback, modeled as an append-only `CashbackRecord` ledger (no wallet balance, no withdrawal — `CashbackStatus.REDEEMED` is a declared-but-unused extension point). `GET /me/referrals` + new `/referrals` web page (code, totals, referral/cashback history). Self-referral and invalid codes are silent no-ops, never a registration failure.

**Master Subscription System (`modules/subscriptions`)** — `FREE`/`PREMIUM` plan × `ACTIVE`/`TRIAL`/`EXPIRED`/`CANCELLED` status, no payment provider involved (`upgradeToPremium` is a pure DB write, 14-day trial, zero `PaymentProvider` calls per explicit scope). An expired trial **self-heals to `EXPIRED` on read** (`getStatus`, no cron), mirroring `TrustService`'s "recompute on read" shape. Premium's "higher visibility/priority ranking" is wired into the **existing** M3 dispatch scoring as a flat `PREMIUM_SCORE_BONUS = 0.1` additive term in `scoreCandidate` (`dispatch-scoring.ts`) — not a separate Premium-first cascade round, since that would be new architecture for a benefit that reduces to "rank slightly higher." `dispatch-eligibility.ts`'s SQL gained a `LEFT JOIN master_subscriptions` + `COALESCE(...) AS "isPremium"` (the `COALESCE` matters — without it, a master with no subscription row gets SQL `NULL`, not `false`, through the `AND`/`=` chain).

**Master Analytics (`modules/analytics`)** — `GET /me/master/analytics` composes real data only from `TrustService`, `ReviewsService`, `SubscriptionsService`, `PaymentsService` (jobs done, rating + trend, response rate, trust score/tier, subscription plan, earnings summary) — zero fabricated values. New `/master/analytics` ("Statistika") web page.

**Schema additions:** `Review`, `PenaltyRecord`, `GuaranteeClaim`, `Referral`, `CashbackRecord`, `MasterSubscription` models; `PenaltyEventType`, `PenaltySeverity`, `GuaranteeClaimStatus`, `ReferralStatus`, `CashbackSourceType`, `CashbackStatus`, `SubscriptionPlan`, `SubscriptionStatus` enums; 6 new `NotificationType` values. `order-state.ts` gained `CANCELLED_BY_MASTER` reachability from `ASSIGNED`/`EN_ROUTE` (previously a pre-declared-but-unreachable value, same pattern M3 used for `EXPIRED` ahead of its own build). Two migrations (`20260721182811_batch2_trust_safety_growth`, `20260721184325_batch2_order_cancelled_by_master_notification`), both hand-stripped of the same generated-column diff artifact every prior M3+ migration has hit, both verified to leave the `orders.location`/`service_areas.centerPoint` GIST indexes intact.

**A real, non-trivial bug found and fixed during this batch's own build (not a tooling quirk):** wiring `ReferralsService` into `AuthModule` (to record a referral signup at registration) closed a genuine 4-module circular import: `AuthModule → ReferralsModule → NotificationsModule → RealtimeModule → AuthModule` (Realtime needs Auth's `JwtService`; Notifications needs Realtime; Referrals needed Notifications to send the reward push). This crashed Nest's boot outright (`UndefinedModuleException: the module at index [0] of the ReferralsModule "imports" array is undefined`) on every restart. Two `forwardRef()` patches at the outer edges did **not** fix it (a 4-node cycle needs every edge wrapped, not just two, and untangling exactly which edges is fragile). Fixed at the root instead: `ReferralsService` no longer depends on `NotificationsService` at all — `rewardOnFirstPayment` now returns the referrer's `userId` (or `null`) instead of sending the notification itself, and `PaymentsService.settle()` (which already depends on both `NotificationsService` and `ReferralsService`) sends the reward notification. This removes the cycle structurally rather than papering over it, and is arguably the better design anyway — a growth-feature service has no business owning a notification side-effect that its caller can trivially perform. Verified by a clean `Nest application successfully started` boot log and all 118 tests (including the referral race tests) passing afterward.

**Verified live, in a real browser + curl (the sandbox browser can't drive multi-step OTP/dispatch flows cheaply, so state setup was curl-driven per this doc's own "Known issues" convention, with browser used for visual/rendering confirmation):** a real customer→customer referral signup, OTP-verify (PENDING→ACTIVE), and `/referrals` page correctly showing the real code, count, and masked phone; a real master registration → verification submit → admin approve → trust tier visibly moving `T0→T1` on `/master` (with the verified checkmark badge) and `/master/analytics`; a real Premium upgrade showing the "Premium faol (sinov muddati)" trial banner and `Premium` badge on the analytics page. Review/guarantee-claim UI was verified by code inspection (correctly wired to the existing `Alert`/`Rating`/`Badge` components, matching the M4/M5 pattern) plus their dedicated passing integration tests, rather than a full manual dispatch→completion→payment lifecycle rebuild for this pass.

**Deliberately scoped down, per explicit batch instructions:** no admin dashboard UI (Batch 3); no dispute-resolution flow for guarantee claims beyond `OPEN`/`UNDER_REVIEW`/`APPROVED`/`REJECTED`; no automatic bans/suspensions from penalties; no cashback withdrawal/redemption; no fraud detection on referrals; no real payment-provider billing for Premium subscriptions.

## What Batch 3 shipped (2026-07-22)

Operations & Scale — a real admin back office over Batches 1–2's data, additive only: **no Batch 1/2 source file was modified for business logic**, per explicit instruction. The two narrow exceptions, both additive: `admin.controller.ts` (Batch 2's existing verification/guarantee decide endpoints) gained audit-logging calls and new `GET` list endpoints alongside its unchanged `decide()` calls; `catalog.controller.ts` and `infra/queue/queue.module.ts` gained a response header and BullMQ retry config respectively (see Performance/Reliability below).

**Admin dashboard (`modules/admin/`, `apps/web/app/admin/`)** — `AdminUsersService` (search/filter/paginate by phone/role/status/verification/trust-tier/subscription, detail, suspend/restore), `AdminOrdersService` (filter by status/category/service-tier/date-range/geography, detail with status history/dispatches/payments), `AdminAnalyticsService` (marketplace-wide metrics, see below), plus list endpoints added to the existing `AdminController` for the verification/guarantee-claim queues. Ten new `/admin/*` pages (`admin-users.controller.ts`), gated by `@Roles('ADMIN')` on every controller and a new optional `requiredRole` param on the frontend's `useRequireAuth()` hook (wrong-role users bounce to `/home`, not `/login` — they're authenticated, just not authorized).

**Audit log (`modules/audit/`, new `AuditLog` model)** — a global `AuditService.record(actorId, action, entityType, entityId?, before?, after?)`, append-only, called by every admin mutation: user suspend/restore, verification decide, guarantee-claim decide, feature-flag upsert. `GET /admin/audit-log` + `/admin/audit-log` page. Deliberately called from the *controller* layer for the pre-existing Batch 2 decide endpoints (not injected into `VerificationService`/`GuaranteeService` themselves) — keeps those files untouched.

**Feature flags (`modules/feature-flags/`, new `FeatureFlag` model)** — flat key/enabled/description CRUD, foundation only per explicit scope: nothing reads these to gate behavior yet. Idempotent upsert, audit-logged.

**Support tool (`modules/admin/admin-support.service.ts`)** — one `GET /admin/support/lookup?phone=` composing `AdminUsersService.detail`, a direct payment-history query, and the *existing* `PenaltiesService.getHistory`/`ReferralsService.getSummary` (Batch 2 methods, reused as-is) into a single consolidated view. No "review conversations" tool was built — there is no chat/messaging feature anywhere in the product to review (confirmed against the design-system rebrand's own note); documented here rather than faked.

**Marketplace analytics (`admin-analytics.service.ts`)** — `GET /admin/analytics/overview`: orders created/completed(`CLOSED`)/cancelled, active masters/customers (distinct order participants in the date range), revenue (`SUCCEEDED` payments), average dispatch response time and average job-completion time (both real `AVG(EXTRACT(EPOCH FROM ...))` raw-SQL aggregates, not estimates), customer satisfaction (`AVG(Review.rating)`), verification/subscription/cashback/referral distribution. All direct Prisma aggregate/raw queries — no materialized views (that's ARCHITECTURE's full v3 vision; over-engineering at current scale). Date-range filters apply to the event-count metrics; verification/subscription/referral stats are current-state snapshots, not date-scoped (documented in-code why).

**A real accuracy bug found and fixed during this batch's own live verification:** `MasterProfile.trustTier` only gets persisted at specific Batch 2 trigger points (job close, review, penalty) — verification approval does **not** trigger a recompute (only the master's own `GET /me/master/trust` does, which calls the persisting `recomputeTrustTier`). This meant a freshly-admin-approved master showed a stale `T0` on the admin masters list until they themselves loaded their own trust page. Confirmed live: approved a test master's verification via `/admin/verifications`, then checked `/admin/masters/:id` — showed `T0` instead of the `T1` a live computation gives. Fixed by having `AdminUsersService.detail()` call `TrustService.computeTrustScore()` (the existing *read-only, non-persisting* half of Batch 2's trust API) instead of reading the cached column, for the one-user detail view only — the list view still reads the column (recomputing per-row for a paginated list would add N extra queries, not worth it for an operational overview). No Batch 2 file touched; `TrustService` already exposed exactly the method needed.

**Reliability fix:** `infra/queue/queue.module.ts`'s BullMQ `defaultJobOptions` had no `attempts`/`backoff` at all — a transient Redis/DB blip silently and permanently dropped an `offer-expiry` job. Added `attempts: 3, backoff: {type: 'exponential', delay: 2000}`; the existing `handleOfferExpiry` idempotency guard makes a retry safe.

**Performance:** new `Order(createdAt)` index — confirmed via `EXPLAIN ANALYZE` against a synthetic 30,000-row seed (scattered across Uzbekistan's real extent, then deleted after measuring) that the admin order list uses an `Index Scan Backward` on it instead of a seq scan; the existing `orders_location_gist` and `orders_masterId_status_idx` indexes were confirmed still in active use for the geo pre-filter and the active-masters aggregate respectively. `GET /categories` gained a `cache-control: public, max-age=60` header (same precedent as `media.controller.ts`) — a cheap win for a hot, rarely-changing, unauthenticated list. Admin list-endpoint aggregate queries on `order_status_history`/`payments`/`reviews` currently seq-scan — correct Postgres behavior at today's real row counts (tens of rows), the same "small-table caveat" every prior audit in this codebase has hit; not indexed preemptively per "add indexes only when measurements justify them."

**Security audit of every new admin endpoint:** confirmed `@Roles('ADMIN')` on all 7 new/extended controllers, no bypass path; found and fixed one real gap — `admin.controller.ts`'s verification/guarantee-claim list endpoints originally took an unvalidated `@Query('status') status?: string` cast with `as never` straight into a Prisma query (not exploitable — Prisma parameterizes regardless — but would 500 with a raw Prisma error on a bad value instead of a clean 400, and violated "validate every query param"). Fixed by adding `adminVerificationListQuerySchema`/`adminGuaranteeClaimListQuerySchema` and routing through the existing `ZodValidationPipe`, matching every other new list endpoint's convention. Privilege-escalation guards on `suspend`: blocks targeting an `ADMIN` account and blocks self-suspend (an admin locking themselves out). Verified live: 8/8 new admin `GET` routes and both mutation endpoints return `403` for a real non-admin JWT, `401` for no token at all.

**Reliability/idempotency verified live:** two truly concurrent `POST /admin/users/:id/suspend` calls against the same fresh user (backgrounded curl, not a mocked race) both returned `201`; exactly one performed the real `ACTIVE→SUSPENDED` transition (`alreadySuspended: false` in its audit entry), the other was a safe no-op (`alreadySuspended: true`) — same guarded-`updateMany` pattern as `DispatchService.acceptOffer`/M4's job-transition race fixes, applied fresh here rather than reusing a shared helper (each caller's "expected prior state" differs).

**Verified live, in a real browser + curl:** admin login → `/admin` overview (real counts, honest `—` for satisfaction with zero reviews) → approved a real pending verification via `/admin/verifications` (audit-logged, trust tier update confirmed) → suspended and restored a real master via `/admin/masters/:id` → created and toggled a real feature flag via `/admin/feature-flags` → looked up a master via `/admin/support` (consolidated view, clickable through to their detail page) → confirmed a full page reload on an admin detail route preserves the session and re-renders correctly → confirmed a logged-in `MASTER` account hitting `/admin` directly is bounced to `/home` (not shown any admin data) → regression spot-check on `/master` and `GET /categories` (with its new header) confirmed unaffected. 151/151 backend tests pass (118 pre-existing + 33 new), zero Batch 1/2 test or source files needed changes to stay green.

**Tooling quirk hit during this pass (not an app bug):** the sandboxed preview-browser's `computer` click tool intermittently clicked at coordinates that didn't match the actual DOM element's `getBoundingClientRect()` for this session (confirmed by comparing a screenshot's visible button position against `document.querySelectorAll('button')[...].getBoundingClientRect()` — they disagreed by ~250px vertically at one point). Refs from `read_page` were more reliable than raw screenshot coordinates, and `javascript_tool`-driven `element.click()` was fully reliable throughout. Not a regression in this batch's UI code — same click handler worked correctly once triggered via any reliable path. Real browsers are unaffected (consistent with this doc's existing "Known issues" entries about this sandbox specifically).

**Deliberately scoped down, per explicit batch instructions:** no 2FA/IP-allowlist on admin routes (ARCHITECTURE's full v3 vision for `/admin`; `@Roles('ADMIN')` is the only gate today, same as Batch 2's original two decide endpoints); no materialized views for analytics; no versioned/hot-reloadable rules engine behind feature flags (flat table only); no support-staff role distinct from ADMIN (the brief only specified ADMIN-only RBAC); no "review conversations" tool (no chat feature exists to review).

## Conventions & decisions

- **Validation:** one Zod schema per shape in `packages/contracts`, used by BOTH the API (`ZodValidationPipe`) and the web (react-hook-form / client checks). Add new shapes there.
- **Money:** integer so'm, never floats. Wallet is an append-only ledger (design in ARCHITECTURE §3.2); withdrawable **earnings** and frozen **credits** (cashback) are separate wallet kinds.
- **The 1% is a tax withholding, not commission.** Confirm the legal obligation with a UZ tax adviser before wiring it into settlement (M4). PINFL is stored encrypted; never log or return it in plaintext (`pinflSet: boolean` is exposed, not the value).
- **Data residency (legal):** UZ law requires citizens' personal data on in-country servers; the primary production DB must be hosted in Uzbekistan. Minimize/disclose foreign sub-processors (Claude, FCM).
- **AI assists, never gates** — every AI feature (from M2 on) needs a deterministic fallback.
- **Matching (M3, shipped):** sequential auto-dispatch — filter eligible masters, score, weighted-random pick among the top-N closest, offer one at a time, cascade on decline/timeout. Customers never see a list. Premium-first is now a flat score bonus, not a separate cascade round — see "What Batch 2 shipped".

## Milestone boundaries (deliberately deferred)

- **Reverse geocoding:** no Yandex Geocoder integration yet — customers confirm/type the readable address themselves after using GPS for coordinates.
- **Media uploads:** local disk (`LocalDiskStorage`); the S3 presign/upload pipeline is a later milestone. Same for master certifications/portfolio (M1, still `objectKey`-only).
- **MyID / Didox / real SMS / rules engine / warranty (beyond Handly Guarantee's basic claim lifecycle) / cashback withdrawal / referral fraud detection:** all later milestones per the roadmap. Interfaces (SmsProvider, VerificationProvider, TaxProvider, PaymentProvider, AiProvider, StorageProvider) exist so they slot in without rework. Trust tiers, verification lifecycle, subscriptions, cashback, and referrals themselves shipped in Batch 2; the admin dashboard UI itself shipped in Batch 3 — see "What Batch 3 shipped".
- **CI/CD, ESLint config:** still not set up (`next.config` skips lint during build).
- **2FA/IP-allowlist on `/admin` routes, materialized-view analytics, a versioned hot-reloadable rules engine, a dedicated support-staff role:** all part of ARCHITECTURE's full v3 admin-surface vision, not built in Batch 3 — `@Roles('ADMIN')` is the only current gate. See "What Batch 3 shipped".
- **Real Click/Payme/Uzum credentials/signature verification:** `PaymentMethod` models all four rails but only `MOCK` actually processes (M5) — no merchant accounts exist yet. Live GPS en-route tracking, master-initiated cancellation/dispute flow (beyond the Batch 2 `cancelByMaster` penalty trigger), and an auto-close-after-timeout worker for unconfirmed `COMPLETED` orders are also not built (M4) — see "What Milestone 4 shipped" / "What Milestone 5 shipped" for the full scoped-down list. No real billing for Premium subscriptions (Batch 2) — no payment-provider call is made on upgrade.

## Known issues

Tooling/environment quirks hit during manual verification — none are application defects. Documented so future verification passes don't misdiagnose them as regressions.

- **Sandboxed preview-browser blocks non-GET/POST requests:** the `mcp__Claude_Browser__*` preview tool (not real Chrome) blocks PATCH/PUT/DELETE at its network layer — GET/POST pass through fine. Confirmed via `read_network_requests` (`net::ERR_FAILED` on the PATCH call) and via curl parity (identical requests succeed instantly outside the sandbox). Work around by driving state-changing steps via curl and verifying rendering/GET/POST-only actions in the browser. Real browsers are unaffected.
- **Same sandbox's session cookie is unreliable across a full page reload:** across two verification passes, a `location.reload()`/F5 in that sandbox sometimes dropped the session (bounced to `/login`) and sometimes didn't — confirmed via a direct `fetch('/auth/refresh', {credentials:'include'})` immediately before/after a reload, which succeeds right up to the reload event either way. This points to the sandbox's proxy/context intermittently clearing cookies on navigation, not the app's session logic. Real browsers are unaffected.
- **Same sandbox can't drive native file pickers:** clicking a file-input control (order media upload) opens an OS-level file dialog this tool has no way to interact with (it lacks a `file_upload`-style capability, unlike the `claude-in-chrome` extension's `file_upload` tool). Media upload can't be exercised via a real UI click here — verify it instead with a direct multipart curl against `POST /orders/:id/media`.
- **Possible React StrictMode double-fire on `/auth/refresh` in dev:** manual verification occasionally saw two back-to-back `POST /auth/refresh` calls on a single page load, one `200` and one `400`. Likely React 18 StrictMode's dev-only double-invoke of the silent-refresh bootstrap effect racing against the rotating single-use refresh token — the first call rotates the cookie, the second (using the now-stale token) is rejected. One call has always succeeded and no user-visible failure has been observed, and StrictMode's double-invoke doesn't happen in production builds. Not yet root-caused with certainty — worth a look at the session bootstrap effect in `apps/web` if it's ever seen alongside an actual visible login/session failure.
- **`next dev`'s cache can go stale after a long run of rapid file edits:** during the 2026-07-21 design-system pass, `apps/web`'s dev server started serving a 404 for its own CSS bundle (`/_next/static/css/app/layout.css`) and every page rendered as unstyled raw HTML with a `500` on the page request — after dozens of edits to `tokens.css` and shared components in one sitting. `pnpm typecheck`/`pnpm build` were clean throughout (proving the code was fine), so this was dev-server-state corruption, not a bug. Fixed by `rm -rf apps/web/.next` + restarting `pnpm dev`. If a running dev server ever starts 404ing its own CSS or 500ing on a page it previously served fine, restart it before assuming a regression.
- **The preview-browser screenshot tool can render a blank frame that doesn't reflect real page state:** after a JS-triggered `window.scrollTo()`, a screenshot occasionally came back solid blank (page-background-colored), even on a second attempt. `document.elementFromPoint()` + `getBoundingClientRect()` on the same page at the same scroll position showed the real content correctly laid out with no gaps — so this is a capture/rasterization timing quirk in the tool, not a real rendering bug. Don't conclude "content is missing/broken" from a single blank screenshot after scrolling — verify with `get_page_text` or direct DOM measurement first.
- **A real (now-fixed) frontend bug, not a tooling quirk, but worth flagging so it isn't reintroduced:** `apps/web/lib/api.ts`'s `request()` used to set `Content-Type: application/json` unconditionally, even when `body` was `undefined` — Fastify's body parser rejects that combination outright (`400: "Body cannot be empty..."`). Every `api.post(path, undefined, true)` call (accept/decline offer, diagnose, cancel, confirm, and the M4 job-action endpoints) was silently broken for real browser clicks, though curl-driven verification never hit it (curl calls in this codebase's own test scripts never set that header without a body either). Caught live during M4 verification when clicking "Qabul qilish" for the first time in an actual browser. Fixed by only setting `Content-Type` when `body !== undefined`. If a new `api.post`/`api.patch` call with no body starts returning `400`, this is the first thing to check.
- **`pkill -f "nest start"` does not match the actual `nest start --watch` process** — its real command line is `.../@nestjs/cli/bin/nest.js start --watch`, and `-f` matches the substring literally, so `"nest start"` never matches (there's a `.js ` between `nest` and `start`, not a space). Killing dev servers with that pattern leaves the parent `nest.js` process (and the `dist/main` child it manages) running, so a fresh `pnpm dev` collides on port 3001 (`EADDRINUSE`) with a stale instance still handling requests — this happened three times in one session and, once, produced a genuinely confusing false lead (a real dispatch bug looked at first like it might be caused by testing against a stale/duplicate process, since `ps aux` showed 3 overlapping `nest.js start --watch` instances at once). Correct patterns: `pkill -9 -f "nest\.js"` and `pkill -9 -f "apps/api/dist/main"`, plus `lsof -ti :3000 :3001 | xargs -r kill -9` as a belt-and-suspenders check — then confirm with `ps aux | grep -iE "nest\.js|next dev|turbo run dev|dist/main"` showing nothing before relaunching.
- **The preview-browser's `computer` click tool can click the wrong coordinates relative to the actual DOM** (hit during Batch 3 verification): a screenshot-based click on a visible button sometimes silently landed elsewhere — confirmed by comparing the click coordinate against the real button's `getBoundingClientRect()` via `javascript_tool` (they disagreed by ~250px in one case), and by `read_network_requests` showing no request fired at all after the click. `read_page` ref-based clicks were more reliable but not immune. The one fully reliable method: `javascript_tool` executing `element.click()` directly on the DOM node (found via `querySelector`/`querySelectorAll`). If a button click in this sandbox appears to do nothing (no network request, no state change), don't conclude the app is broken — retry via direct DOM `.click()` before investigating further. Real browsers are unaffected.

## Key env vars

`DATABASE_URL`, `REDIS_URL`, `JWT_ACCESS_SECRET`, `JWT_ACCESS_TTL`, `JWT_REFRESH_TTL_DAYS`, `FIELD_ENCRYPTION_KEY`, `SMS_PROVIDER` (`mock`|`eskiz`) + `ESKIZ_*`, `TAX_PROVIDER` (`mock`|`soliq`) + `TAX_WITHHOLDING_RATE`, `WEB_ORIGIN`, `NEXT_PUBLIC_API_URL`, **`AI_PROVIDER`** (`mock`|`claude`) + `ANTHROPIC_API_KEY` + `AI_MODEL` + `AI_TIMEOUT_MS`, **`UPLOAD_DIR`** + `UPLOAD_MAX_PHOTO_MB` + `UPLOAD_MAX_VIDEO_MB` + `ORDER_MAX_MEDIA`, **`PUSH_PROVIDER`** (`mock`|`fcm`) + `FCM_PROJECT_ID` + `FCM_CLIENT_EMAIL` + `FCM_PRIVATE_KEY`, **`DISPATCH_TOP_N`** + `DISPATCH_RADIUS_EXPANSION_FACTOR` + `DISPATCH_OFFER_TTL_SCHEDULED_SECONDS` + `DISPATCH_OFFER_TTL_PRIORITY_SECONDS` + `DISPATCH_OFFER_TTL_EMERGENCY_SECONDS`, **`LOGIN_MAX_ATTEMPTS`** + `LOGIN_LOCKOUT_SECONDS`, **`PAYMENT_PROVIDER`** (`mock` only — Click/Payme/Uzum need real merchant credentials that don't exist yet). See `.env.example`.
